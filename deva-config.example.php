<?php
/* ============================================================
   deva-config.example.php — MODÈLE de configuration de Deva
   ------------------------------------------------------------
   1. Copiez ce fichier en "deva-config.php" (SANS .example)
      dans le même dossier que deva.php (racine de evad.org).
   2. Remplacez la valeur par votre clé Mistral (console.mistral.ai).
   3. deva-config.php ne doit JAMAIS être commité (il est déjà
      dans le .gitignore). Il reste uniquement sur le serveur.

   Alternative : définir la variable d'environnement
   MISTRAL_API_KEY (hPanel / .htaccess) et ne pas créer ce fichier.
   ============================================================ */

return 'VOTRE_NOUVELLE_CLE_MISTRAL_ICI';
